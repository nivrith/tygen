import {
  {{name.camel}}
} from './../src/index';
import {
  expect
} from 'chai';

describe('{{name.camel}}', () => {

  it('Returns `hello universe`', () => {
    expect({{name.camel}}()).to.equal('hello universe');
  });

});
